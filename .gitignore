# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv
worktrees/
results/
queue/

# Agent prompt files (generated per-session by launchers)
CLAUDE.md
AGENTS.md

# Experimental code/artifacts
dev/

# Autoresearch runtime outputs (never commit these)
run.log
strategy_metrics.json
prepared_market_data.csv
# results.tsv IS committed — it is the persistent experiment log
